# Stoner's Design

Use the skills in `skills/` as a design toolkit. Select only the skills relevant to the user's request, preserve the user's constraints, and move from product intent to polished implementation. The `stoners-design` MCP server can list, search, and load the collection on demand.
